services:
  rbac_app:
    build:
      context: ..
      dockerfile: examples/rbac_app/Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - rbac_db:/app/data
    environment:
      - DATABASE_URL=sqlite:////app/data/rbac.db

volumes:
  rbac_db:
