# Gemini

This file contains information about the Gemini project.

## Command Execution

When executing commands, always use `poe` or `uv run`. For example, to run tests, use `poe test` instead of `pytest`.
